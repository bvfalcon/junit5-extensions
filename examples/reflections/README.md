# JUnit5-Extensions Reflections Example
The simplest demonstraion of using Reflections [junit5-extensions](https://github.com/bvfalcon/junit5-extensions).

Run it with command `mvn clean test` and see the results
