# JUnit5-Extensions Annotations Example
The simplest demonstraion of using annotations [junit5-extensions](https://github.com/bvfalcon/junit5-extensions).

Run it with command `mvn clean test` and see the results
